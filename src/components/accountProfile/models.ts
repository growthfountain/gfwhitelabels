import { AnonymousUser } from 'src/components/anonymousAccount/models.ts';
import { Model } from 'src/core/models.ts';

/*
export class User extends from Model {
  public baseUrl:string = app.config.authServer + '/rest-auth/data';
}
 */
